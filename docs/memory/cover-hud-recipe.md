# COVER HUD OVERLAY WORKFLOW (approved 2026-08-31, user: 'ja passt, genial')

Problem: AI image model bakes garbled text into covers (5 .webp covers garbled). Fix: generate text-free image, overlay text AFTER via Playwright.

FILES (in /home/crd-remote/suno-worker/):
- render-cover-hud.mjs — renderer. Args: node render-cover-hud.mjs <baseImg> <ARTIST> "<TITLE>" <out>. 1024x1024 headless chromium. Layout (APPROVED, DO NOT CHANGE without request): (a) base img cover-fill, (b) .vig dark gradient top+bottom, (c) cyberpunk HUD frame (4 corner brackets + 4 h/v lines + ticks, cyan accent), (d) .accent top gradient line, (e) '◆ ARTIST' badge top-left Orbitron, segline top-right, (f) title bottom-left Orbitron 900 gradient text + gradient rule divider, (g) logo portal-A symbol ONLY bottom-right (NO text wordmark), (h) NO track serial (removed per user). Uses artist config + Google Fonts Orbitron.
- cover-hud.json — per-artist config: { artists:{ <NAME>:{ style, accent, accent2, brandColor, titleGradient[3] } }, fonts:{display:'Orbitron'}, brand:'ANDRA NETWORK', brandSvg:'/tmp/opencode/brand/portal-a-symbol-negative.svg' }. Only BUNDIX defined: accent #7df9ff.
- Supabase tracks table has `version` field = per-artist track serial. NOT shown on cover (user: 'lasse track serial weg').

Brand SVGs hosted live at https://andranet.ch/brand/*.svg (portal-a-symbol-negative.svg = white symbol). /tmp/opencode/brand/ has them downloaded.

VERIFY: tesseract OCR (text clean, no garble) + PIL brightness regions (model can't view images). User views in Chrome via `google-chrome --no-sandbox --new-window file://.../xxx.png`.

ROLL-OUT DONE (2026-08-31): (1) cover-hud.json filled for ALL 6 artists: BUNDIX cyan #7df9ff, ANDRAMON magenta #c75bff (psy), ANDRAX red #ff5a3c (hardstyle), DYBUN green #7dff5a (acid techno), AERYN lavender #b39dff (ambient), NALDIX gold #ffd166 (deep house). Verified accent per artist renders (corner RGB sampled). (2) PIPELINE INTEGRATED: render-cover-hud.mjs refactored to module exporting `renderHudCover({baseImg,artist,title,out})` + guarded CLI (import.meta.url check). finalize-gp.mjs genCover(prompt,{artist,title}) now writes SD png to tmp, runs renderHudCover, reads result, cleans up — HUD overlay applied before Supabase upload. Verified via import test + node --check. (3) REGEN 5 garbled .webp covers DONE 2026-08-31: AURORA GLITCH ENGINE, CRYSTAL DRIFT SEQUENCE, NEON PULSE PROTOCOL, UPLIFTING WAVE, METALLIC TENSION CIRCUIT. Used HUD overlay on original .webp files (preserves user-preferred original style). Uploaded via curl PUT to https://sgvguaaccmzevipfwdbn.supabase.co/storage/v1/object/tracks/covers/<id>.png (bucket=tracks, NOT /public/). Files at /tmp/orig_covers/hud_*.png. All 5 verified HTTP 200. Supabase service_role key eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InNndmd1YWFjY216ZXZpcGZ3ZGJuIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4MTE5OTc0OCwiZXhwIjoyMDk2Nzc1NzQ4fQ.X1EKR4_F8SWrVfk9xWtRYCUggZOc6vJY8XMKB7Th-_E WORKING 2026-08-31. Upload recipe: curl -X PUT -H "apikey: $KEY" -H "Authorization: Bearer $KEY" -F "file=@local" $BASE/covers/<id>.png (PUT replaces, POST fails 409).
